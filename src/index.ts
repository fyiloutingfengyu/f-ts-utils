import dateFormat from './date-formate';
import {
  getLocalStorage,
  setLocalStorage,
  removeLocalStorage
} from './storage';

const fUtils = {
  dateFormat,
  getLocalStorage,
  setLocalStorage,
  removeLocalStorage
};

export {
  dateFormat,
  getLocalStorage,
  setLocalStorage,
  removeLocalStorage
};

export default fUtils;
